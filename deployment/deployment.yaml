apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-app-deployment
  labels:
    app: python-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: python-app
  template:
    metadata:
      labels:
        app: python-app
    spec:
      containers:
      - name: python-app
        image: vyshakprojects/demo-image-test:1953cb772028d483da6bea1856f6d008278b43e7
        ports:
        - containerPort: 8000  # Update with the port your app listens on
        env:
        - name: ENV_VAR_NAME  # Add any environment variables if needed
          value: "your_value"
